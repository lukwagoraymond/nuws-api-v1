Use url = 'https://kf.kobotoolbox.org/api/v2/assets/' to access the api v2 assets
This is a json containing information on the forms, submissions and exports of data to various formats like csv, xlsx, number of downloads, name of the form.

Use url = 'https://kf.kobotoolbox.org/token/' to get token if Basic authentication is enabled.

Use url = 'https://kc.kobotoolbox.org/api/v1/data' to get actual data submitted

Use url 'https://kc.kobotoolbox.org/api/v1/data/1330078' to get NUWS form data submitted

columns to drop = ["start", "__version__", "meta/instanceID", "_xform_id_string", '_attachments', '_status', '_geolocation', '_submission_time', '_tags', '_notes', '_submitted_by']

Create the db name, user and password with the script like this:
cat setup_mysql_db.sql | mysql -hlocallost -uroot -p
Enter Password: 
Now check for the database name if it is created like this:
echo "SHOW DATABASES;" | mysql -u nuws_dev -p | grep nuws_data_db
Enter Password:
Nows check for the grant permissions to the user created like this:
echo "SHOW GRANTS FOR 'nuws_dev'@'localhost';" | mysql -u root -p
Enter Password

f"mysql+mysqldb://{MYSQL_DB['DB_USER']}:"
                                      f"{MYSQL_DB['DB_PASS']}@{MYSQL_DB['DB_HOST']}"
                                      f"/{MYSQL_DB['DB_NAME']}"
                                      
When executing either the main.py or app.py use the python3 -m <module_name> inorder to correctly allow python to locate the location of the modules in other packages. Using the method python3 <module_name.py> or ./<module_name.py> could work but in other cases causes errors since python finds it difficult to locate the packages.
